---
# ctorgalson.files vars.
files_files:
  - path: "/Users/travis/Dev"
    state: directory
    owner: "{{ mb_user }}"
    group: "{{ mb_group }}"
  - path: "/Users/travis/Dev/travis"
    state: directory
    owner: "{{ mb_user }}"
    group: "{{ mb_group }}"
